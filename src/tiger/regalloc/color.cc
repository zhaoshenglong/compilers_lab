#include "tiger/regalloc/color.h"

namespace COL {

Result Color(G::Graph<TEMP::Temp> ig, TEMP::Map initial, TEMP::TempList regs,
             LIVE::MoveList* moves) {
  
  return Result();
}
  
}  // namespace COL